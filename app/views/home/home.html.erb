<%=link_to "Sign In", '/login'%>
<%=link_to "Create an Account", 'users/new'%>
<%=link_to "IronDocs™ Stats", 'stats/index'%>

<br>
<%=form_tag public_search_path do%>
    <%=text_field_tag 'search'%>
    <%=submit_tag "Search for Public Files"%>
<%end%>

<% if @projects.count > 0 %>
    <h3>Projects</h3>
    
    <li>
    <% @projects.each do |p|%>
        <ul>
        <li>Title: <%= link_to p.title, project_path(p)%></li>
        <li>Description: <%=p.description%></li>
        <hr>
        </ul>
    <%end%>
    </li>
<%end%>

<% if @users.count > 0 %>
    <h3>Users</h3>
    <% @users.each do |u|%>
        <li><%= link_to u.username, user_path(u)%></li>
    <%end%>
<%end%>